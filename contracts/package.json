{
  "name": "defi-borrowing-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for DeFi borrowing protocol switching (Foundry)",
  "scripts": {
    "test": "forge test -vvv",
    "build": "forge build",
    "deploy:switcher": "forge script script/DeploySwitcher.s.sol --rpc-url $RPC_URL --broadcast",
    "deploy:paymaster": "forge script script/DeployPaymasterSimple.s.sol --rpc-url $RPC_URL --broadcast"
  }
}
